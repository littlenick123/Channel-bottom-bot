services:
  bottom-post-bot:
    build: .
    restart: unless-stopped
    env_file: ${ENV_FILE:-.env}
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
